// JWT Client Browser Authentication Parser
if(localStorage.getItem("accessToken")) {
	console.log("access tokenw found in localStorage");
}
else {
	console.log("access token does not exist in localStorage");
}